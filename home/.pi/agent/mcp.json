{
    "mcpServers": {
        "executor": {
            "type": "http",
            "url": "http://localhost:4789/mcp?elicitation_mode=native",
            "headers": {
                "Authorization": "Bearer ${EXECUTOR_API_KEY}"
            }
        }
    }
}
